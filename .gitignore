# Saída gerada: contém nome/contrato de beneficiários reais
prontos/

# Arquivos de lock do Word (abertos durante a edição dos modelos)
~$*

# Backups gerados por corrigir_modelo.py (o original está no histórico do git)
*.docx.bak

__pycache__/
*.pyc

# Instruções para o Claude Code: só deve existir localmente na pasta main
CLAUDE.md

# Find/: PDFs de reajuste (2022, 2023) e saídas com dados de beneficiários reais.
# Só o conteúdo é ignorado; as pastas ficam versionadas vazias via .gitkeep.
Find/2022/*
!Find/2022/.gitkeep
Find/2023/*
!Find/2023/.gitkeep
Find/output/*
!Find/output/.gitkeep
